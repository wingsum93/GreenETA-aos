Use Kotlin + Compose.
Prefer small, reviewable diffs.
When uncertain, ask in comments inside the PR (or leave TODO markers with context).

Do:
- use Dto suffix to class that is API payloads
- use domain class (naming is not restricted) for internal use and display